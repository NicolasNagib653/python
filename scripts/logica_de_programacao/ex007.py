# Calculo de valor de imposto com uma taxa de 60% de juros

valor = float(input("Qual o valor dos produtos? "))
taxa = valor + valor*60/100

print("O valor atual do produto é U$", valor, "já o valor com o imposto será de U$", taxa)